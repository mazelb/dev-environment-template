# Multi-Archetype System Architecture

Visual diagrams explaining the multi-archetype composition system.

---

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    dev-environment-template                     │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │             create-project.sh (Enhanced)                 │  │
│  │  --archetype base --add-features f1,f2 --dry-run       │  │
│  └─────────────────┬───────────────────────────────────────┘  │
│                    │                                            │
│         ┌──────────┼──────────┐                                │
│         ▼          ▼          ▼                                │
│  ┌──────────┐ ┌─────────┐ ┌──────────────┐                   │
│  │ Archetype│ │Conflict │ │Documentation │                   │
│  │  Loader  │ │Resolver │ │  Generator   │                   │
│  └──────────┘ └─────────┘ └──────────────┘                   │
│         │          │          │                                │
│         └──────────┼──────────┘                                │
│                    ▼                                            │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                   archetypes/                            │  │
│  │  ├─ base/                                               │  │
│  │  ├─ rag-project/                                        │  │
│  │  ├─ agentic-workflows/                                  │  │
│  │  ├─ monitoring/                                         │  │
│  │  └─ rag-agentic-system/ (composite)                    │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                   ┌──────────────────┐
                   │  Generated       │
                   │  Project         │
                   │                  │
                   │  my-app/         │
                   │  ├─ src/         │
                   │  ├─ tests/       │
                   │  ├─ docker-      │
                   │  │   compose.*   │
                   │  ├─ Makefile     │
                   │  └─ COMPOSITION  │
                   │      .md         │
                   └──────────────────┘
```

---

## Layered Composition Flow

```
┌──────────────────────────────────────────────────────────────┐
│ User Command:                                                │
│ ./create-project.sh --name myapp                            │
│   --archetype rag-project                                   │
│   --add-features agentic-workflows,monitoring               │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. Load Metadata                                            │
│    ├─ rag-project/__archetype__.json                       │
│    ├─ agentic-workflows/__archetype__.json                 │
│    └─ monitoring/__archetype__.json                        │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. Detect Conflicts                                         │
│    ✓ Port conflicts: 3 found                               │
│    ✓ Service names: 2 collisions                           │
│    ✓ Dependencies: 1 version mismatch                      │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. Resolve Conflicts                                        │
│    ├─ Apply port offsets                                   │
│    │  • rag-project: 8000, 9200         (offset: 0)       │
│    │  • agentic-workflows: 8100, 8180   (offset: +100)    │
│    │  • monitoring: 9090, 3000          (offset: +200)    │
│    ├─ Prefix service names                                 │
│    │  • rag_api, rag_opensearch                           │
│    │  • agentic_api, agentic_airflow                      │
│    │  • monitoring_prometheus, monitoring_grafana         │
│    └─ Resolve dependencies                                 │
│       • langchain: 0.1.0 (base) + 0.2.0 (feature)         │
│         → Resolved: 0.1.5 (compatible)                     │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. Copy & Merge Files                                       │
│    ├─ Copy base (rag-project/)                             │
│    ├─ Merge feature 1 (agentic-workflows/)                 │
│    │  └─ Apply offset, prefix, merge configs              │
│    └─ Merge feature 2 (monitoring/)                        │
│       └─ Apply offset, prefix, merge configs              │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. Generate Documentation                                   │
│    └─ Create COMPOSITION.md with:                          │
│       • Architecture diagram                               │
│       • Service URLs & ports                               │
│       • Makefile commands                                  │
│       • Troubleshooting guide                              │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│ ✓ Project Created: myapp/                                  │
│   ├─ docker-compose.yml                                    │
│   ├─ docker-compose.rag.yml                                │
│   ├─ docker-compose.agentic.yml                            │
│   ├─ docker-compose.monitoring.yml                         │
│   ├─ Makefile (merged)                                     │
│   ├─ .env (merged)                                         │
│   ├─ requirements.txt (resolved)                           │
│   ├─ COMPOSITION.md                                        │
│   └─ src/, tests/, docker/                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## Conflict Resolution Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    Conflict Types                          │
└────────────┬────────────┬────────────┬──────────┬──────────┘
             │            │            │          │
    ┌────────▼──┐  ┌─────▼─────┐  ┌──▼────┐  ┌──▼────────┐
    │  Ports    │  │  Services │  │ Deps  │  │  Files    │
    └────────┬──┘  └─────┬─────┘  └──┬────┘  └──┬────────┘
             │            │            │          │
             └────────────┼────────────┼──────────┘
                          │            │
                          ▼            ▼
              ┌───────────────────────────────────┐
              │    Conflict Resolver              │
              │                                   │
              │  Port Offsetting:                 │
              │  ┌─────────────────────────────┐  │
              │  │ base:     8000              │  │
              │  │ feature1: 8000 + 100 = 8100 │  │
              │  │ feature2: 8000 + 200 = 8200 │  │
              │  └─────────────────────────────┘  │
              │                                   │
              │  Service Prefixing:               │
              │  ┌─────────────────────────────┐  │
              │  │ api → rag_api               │  │
              │  │ api → agentic_api           │  │
              │  └─────────────────────────────┘  │
              │                                   │
              │  Dependency Resolution:           │
              │  ┌─────────────────────────────┐  │
              │  │ v1 ∩ v2 = compatible v      │  │
              │  └─────────────────────────────┘  │
              │                                   │
              │  File Merging:                    │
              │  ┌─────────────────────────────┐  │
              │  │ .env:      append           │  │
              │  │ Makefile:  namespace        │  │
              │  │ compose:   deep merge       │  │
              │  └─────────────────────────────┘  │
              └───────────┬───────────────────────┘
                          │
                          ▼
                ┌──────────────────┐
                │ Resolved Project │
                └──────────────────┘
```

---

## Example: RAG + Agentic Architecture

```
┌───────────────────────────────────────────────────────────────┐
│                      Client Application                       │
└────────────────────────┬──────────────────────────────────────┘
                         │
              ┌──────────┴──────────┐
              │                     │
              ▼                     ▼
┌──────────────────────┐  ┌──────────────────────┐
│   RAG Archetype      │  │ Agentic Archetype    │
│   (Base, offset 0)   │  │ (Feature, offset +100)│
├──────────────────────┤  ├──────────────────────┤
│ Services:            │  │ Services:            │
│ • rag-api :8000     │  │ • agentic-api :8100 │
│ • opensearch :9200  │  │ • airflow :8180     │
│ • ollama :11434     │  │ • flower :5655      │
├──────────────────────┤  ├──────────────────────┤
│ Capabilities:        │  │ Capabilities:        │
│ • Vector search      │  │ • Agent orchestration│
│ • Embeddings         │  │ • Workflows          │
│ • RAG pipeline       │  │ • Task scheduling    │
└──────────┬───────────┘  └──────────┬───────────┘
           │                         │
           └────────┬────────────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │  Shared Resources     │
        ├───────────────────────┤
        │ • PostgreSQL :5432    │
        │ • Redis :6379         │
        └───────────────────────┘

Generated Files:
├─ docker-compose.yml          (base services)
├─ docker-compose.rag.yml      (RAG services)
├─ docker-compose.agentic.yml  (Agentic services)
├─ Makefile                    (merged commands)
└─ .env                        (merged config)

Makefile Commands:
├─ make start                  (all services)
├─ make rag-start             (RAG only)
├─ make agentic-start         (Agentic only)
├─ make test                  (all tests)
├─ make rag-test              (RAG tests)
└─ make agentic-test          (Agentic tests)
```

---

## Port Mapping Strategy

```
┌──────────────────────────────────────────────────────────┐
│                   Port Allocation                        │
└──────────────────────────────────────────────────────────┘

Base Archetype (offset 0):
8000 ─────────► API
9200 ─────────► Database/Search
11434 ────────► Model Service

Feature 1 (offset +100):
8100 ─────────► API         (was 8000)
9300 ─────────► Database    (was 9200)
11534 ────────► Service     (was 11434)

Feature 2 (offset +200):
8200 ─────────► API         (was 8000)
9400 ─────────► Database    (was 9200)
11634 ────────► Service     (was 11434)

Feature 3 (offset +300):
8300 ─────────► API
9500 ─────────► Database
11734 ────────► Service

Maximum Features: 10 (offsets 0-900)

Special Cases:
- Shared services (PostgreSQL, Redis): No offset
- System ports (< 1024): Skip offsetting
- Conflicting offsets: Use next available port
```

---

## File Merge Strategy Matrix

```
┌─────────────────────────────────────────────────────────────┐
│                    File Merge Strategies                    │
└─────────────────────────────────────────────────────────────┘

.env:
  Base:    OPENAI_KEY=xxx, OPENSEARCH_HOST=opensearch
  Feature: ANTHROPIC_KEY=yyy, AIRFLOW_HOST=airflow
  Merged:  OPENAI_KEY=xxx
           OPENSEARCH_HOST=opensearch
           ANTHROPIC_KEY=yyy
           AIRFLOW_HOST=airflow

Makefile:
  Base:    run: docker-compose up
  Feature: run: docker-compose up
  Merged:  rag-run: docker-compose -f compose.rag.yml up
           agentic-run: docker-compose -f compose.agentic.yml up
           run: rag-run agentic-run

docker-compose.yml:
  Base:    services: {api, db}
  Feature: services: {api, worker}
  Merged:  services: {api, db, agentic_api, agentic_worker}

requirements.txt:
  Base:    langchain==0.1.0
  Feature: langchain==0.2.0
  Merged:  langchain==0.1.5  (compatible version)

src/api/main.py:
  Base:    app.include_router(search_router)
  Feature: app.include_router(agents_router)
  Merged:  app.include_router(search_router, prefix="/rag")
           app.include_router(agents_router, prefix="/agentic")
```

---

## Composition Decision Flow

```
Start
  │
  ▼
Need to combine archetypes?
  │
  ├─ NO ──► Use single archetype
  │         └─> ./create-project.sh --archetype base
  │
  └─ YES
      │
      ▼
  Is there a pre-configured pattern?
      │
      ├─ YES ──► Use composite archetype
      │          └─> ./create-project.sh --archetype rag-agentic-system
      │
      └─ NO
          │
          ▼
  Do you have a primary archetype?
          │
          ├─ YES ──► Use layered composition
          │          └─> ./create-project.sh --archetype base
          │               --add-features feature1,feature2
          │
          └─ NO ──► All archetypes equal?
                    │
                    ├─ YES ──► Use modular composition [v2.1]
                    │          └─> ./create-project.sh
                    │               --archetypes a1,a2,a3
                    │
                    └─ NO ──► Reconsider design
                               (pick a primary)
```

---

## Data Flow: Project Creation

```
User Input
    │
    │ --archetype rag-project --add-features agentic-workflows
    │
    ▼
┌────────────────────┐
│ Parse CLI Args     │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐     ┌─────────────────┐
│ Load Metadata      │────►│ Validate JSON   │
└────────┬───────────┘     └─────────────────┘
         │
         ▼
┌────────────────────┐     ┌─────────────────┐
│ Detect Conflicts   │────►│ Generate Report │
└────────┬───────────┘     └─────────────────┘
         │
         ▼
┌────────────────────┐
│ User Approval?     │
└────────┬───────────┘
         │ YES
         ▼
┌────────────────────┐     ┌─────────────────┐
│ Copy Base Files    │────►│ Apply Templates │
└────────┬───────────┘     └─────────────────┘
         │
         ▼
┌────────────────────┐     ┌─────────────────┐
│ Merge Features     │────►│ Resolve Conflicts│
└────────┬───────────┘     └─────────────────┘
         │
         ▼
┌────────────────────┐     ┌─────────────────┐
│ Generate Docs      │────►│ COMPOSITION.md  │
└────────┬───────────┘     └─────────────────┘
         │
         ▼
┌────────────────────┐
│ Git Init (optional)│
└────────┬───────────┘
         │
         ▼
    Success! 🎉
```

---

## System Components

```
┌─────────────────────────────────────────────────────────────┐
│            dev-environment-template                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Core Scripts:                                              │
│  ├─ create-project.sh          (main entry point)         │
│  ├─ scripts/                                               │
│  │  ├─ archetype-loader.sh     (load metadata)            │
│  │  ├─ conflict-resolver.sh    (detect & resolve)         │
│  │  ├─ env-merger.sh           (merge .env)               │
│  │  ├─ makefile-merger.sh      (merge Makefiles)          │
│  │  ├─ docker-compose-merger.sh (merge compose)           │
│  │  └─ documentation-generator.sh (generate docs)         │
│                                                             │
│  Archetypes:                                                │
│  ├─ archetypes/                                            │
│  │  ├─ base/                   (minimal template)         │
│  │  ├─ rag-project/            (RAG system)               │
│  │  ├─ agentic-workflows/      (multi-agent)              │
│  │  ├─ monitoring/             (observability)            │
│  │  ├─ api-gateway/            (NGINX gateway)            │
│  │  └─ rag-agentic-system/     (composite)                │
│                                                             │
│  Documentation:                                             │
│  ├─ MULTI_ARCHETYPE_COMPOSITION_DESIGN.md                  │
│  ├─ MULTI_ARCHETYPE_RESEARCH_SUMMARY.md                    │
│  ├─ docs/                                                  │
│  │  ├─ MULTI_ARCHETYPE_EXAMPLES.md                        │
│  │  ├─ IMPLEMENTATION_GUIDE.md                            │
│  │  ├─ STRATEGY_COMPARISON.md                             │
│  │  └─ QUICK_REFERENCE.md                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

**For full details, see:**
- Design: `MULTI_ARCHETYPE_COMPOSITION_DESIGN.md`
- Examples: `docs/MULTI_ARCHETYPE_EXAMPLES.md`
- Implementation: `docs/IMPLEMENTATION_GUIDE.md`
